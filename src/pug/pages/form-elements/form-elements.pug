extend ../../layout/ui-kit

block variables
 - var title = 'Form Elements';
 - var description = 'Form Elements Description';
 - var pageClass = 'form-elements';

block content

block script
 script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }