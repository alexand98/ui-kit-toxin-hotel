extend  ../../layout/ui-kit

block variables
 - var title = 'Colors and Type title';
 - var description = 'Color and Type description';
 - var pageClass = 'colors-and-type';

block content
 include ../../modules/palette/palette
 include ../../modules/tags/tags

block script
 script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }
 

